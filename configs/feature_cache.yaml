# Cache versioning for preprocessed Parquet artifacts.
#
# Increment `feature_version` whenever feature engineering logic changes
# (e.g., new lag columns, different rolling window, changed SNAP handling).
# prepare_dataset.py will automatically reprocess any store whose cached
# Parquet file has a mismatched or missing version marker.

feature_version: 2
